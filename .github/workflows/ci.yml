name: CI
on: [push, pull_requestnnn
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  
bbbbbbbbbbbb
      - uses: actions/setup-node@v4
        with:
          node-version: '16'

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build
